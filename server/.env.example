# Port du serveur Node.js
PORT=4000

# Environnement (development, production)
NODE_ENV=development

# URL de connexion à la base MySQL (adapter utilisateur/motdepasse/port/nom_bdd)
DATABASE_URL=mysql://user:password@localhost:3306/trouve_ton_artisan

# Origine autorisée pour le CORS (client React)
CORS_ORIGIN=http://localhost:5173

# Config SMTP (mail)
SMTP_HOST=smtp.exemple.com
SMTP_PORT=587
SMTP_USER=ton_email@exemple.com
SMTP_PASS=ton_mot_de_passe
SMTP_FROM="Trouve ton artisan <no-reply@exemple.fr>"
